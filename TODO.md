# 판서 기능 TODO (MVP)

아래 체크리스트는 근무자(송출) 웹앱과 서버(관제) 웹앱에 판서 기능을 추가하기 위한 우선 구현 항목입니다. 기존 흩어져 있던 판서 관련 TODO는 이 문서에 통합합니다.

## 근무자 웹앱 뷰
- [ ] 웹페이지 레이아웃을 판서 우선으로 재구성하고 영상 표시 영역을 최대한 크게 확보
- [ ] WebRTC 영상 위에 오버레이 형태의 UI 버튼 배치
  - [ ] "촬영 시작/종료" 버튼을 기존 스트림 start/stop 로직과 연동
  - [ ] "판서 시작/종료" 토글 버튼 추가
- [ ] 판서 모드 진입 시 근무자 영상 위에 캔버스 레이어 활성화
- [ ] 마우스/터치 드래그로 선을 그릴 수 있는 기본 판서 도구 구현
- [ ] 고정 팔레트(예: 3~4개 색상) 색상 선택 도구 제공
- [ ] 판서 내용이 근무자 측에서 영상과 합성되어 WebRTC로 송출되도록 스트림 처리 로직 수정
  - [ ] `<video>` + `<canvas>`를 하나의 `<canvas>`에 그린 뒤 `canvas.captureStream()`으로 새로운 MediaStream을 생성
  - [ ] PeerConnection 트랙을 합성 스트림으로 교체하고, 캔버스 리사이즈/좌표 스케일 조정을 처리

## 서버 웹페이지
- [ ] 접속 시 기본적으로 "판서 대기 상태"로 동작하도록 UI/상태 구성
- [ ] WebRTC 수신 영상 위에 캔버스 오버레이 레이어를 항상 올려두고 기본 드래그 판서를 지원
- [ ] 판서 색상은 항상 파란색(#0000FF) 고정 (팔레트 노출 없음)
- [ ] 서버가 그린 선은 로컬 오버레이에만 표시하고 영상 스트림으로 재인코딩하지 않음

## 판서(공통 로직)
- [ ] 좌표 전송 경로 정의: DataChannel이 없으므로 새 RTCDataChannel을 생성하거나 기존 Socket.IO 채널을 재사용하여 판서 좌표를 송수신
- [ ] 근무자 측 클릭·드래그 좌표를 서버로 전송하고, 서버에서는 합성된 영상만 확인하면 되므로 별도 렌더링이 불필요함을 명시
- [ ] 서버 측 클릭·드래그 좌표를 근무자 측으로 전송하여 근무자 화면에도 서버 판서를 캔버스 오버레이로 표시 (영상에 재합성하지 않음)
- [ ] 양쪽에서 stroke 데이터를 관리할 in-memory 구조 설계 (예: `{points, color, createdAt}` 배열)
- [ ] 모든 스트로크에 생성 시각(`createdAt`)을 부여하고 10초 유지 후 자동 삭제하는 타이머 로직 추가
- [ ] 10초 경과한 스트로크를 양쪽 모두에서 제거하도록 동기화 방식 정의 (동일 ID/타임스탬프 기반 삭제 이벤트 공유)
- [ ] 좌표 전송 시 뷰포트/캔버스 크기 차이를 고려한 정규화/역정규화 로직 포함

## 제외 범위(MVP 밖)
- [ ] Undo/Redo 기능
- [ ] 전체 지우기 버튼
- [ ] 판서 로그의 영구 저장 또는 서버 저장소 연동
- [ ] 멀티 컬러·멀티 레이어 등 복잡한 화이트보드 기능
