version: '3.8'

services:
  webrtc-server:
    image: webrtc-server:latest
    container_name: webrtc-minimal
    ports:
      - "0.0.0.0:3000:3000"
    environment:
      - PORT=3000
      - HOST=0.0.0.0
    restart: unless-stopped
