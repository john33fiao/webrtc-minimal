# Agent Instructions for `webrtc-minimal`

이 문서는 저장소 전반에 적용되는 작업 지침입니다. 추가로 하위 디렉터리에 별도의 AGENTS.md가 생기면 그 지침을 우선 적용하세요.

## 프로젝트 개요
- Python `aiohttp` + `python-socketio` 서버(`server.py`)와 정적 HTML 클라이언트(`public/`)로 구성된 WebRTC 1:1 영상 스트리밍 샘플입니다.
- HTTPS 개발 서버는 로컬에서 발급한 `cert.pem`/`key.pem`을 사용합니다. (없으면 `python gen_cert.py` 실행)
- 시그널링과 판서 좌표 릴레이는 모두 Socket.IO 이벤트를 통해 이루어집니다.

## 작업 지침
- **언어/표기:** UI 텍스트와 주석은 기본적으로 한국어를 유지합니다.
- **서버 코드(`server.py`):**
  - 단일 룸(`room1`)을 사용하는 현재 구조를 유지하고, 새로운 이벤트도 동일 룸 브로드캐스트 패턴을 따릅니다.
  - WebRTC 시그널링/판서 이벤트 명칭(예: `annotation:point`)을 변경할 때는 클라이언트 양쪽을 함께 업데이트하고 문서화하세요.
  - SSL 관련 예외 처리는 간단한 안내 로그 수준으로 두며, 과도한 제어 흐름 변경을 피합니다.
- **프론트엔드(`public/`):**
  - 추가 스크립트/스타일은 가능하면 각 HTML 파일 내부에 유지하고, 외부 빌드 스텝을 도입하지 않습니다.
  - 판서 입력은 정규화 좌표(0.0~1.0) 기반으로 송수신합니다. 새 로직이 필요하면 송신/수신 양쪽의 정규화·역정규화 경로를 모두 확인하세요.
  - 기존 UI 톤(어두운 배경, 민트/블루 포인트 컬러)과 레이아웃 가이드를 따릅니다.

## 테스트 및 실행
- 기본 확인은 로컬에서 `python server.py` 실행 후 `https://localhost:3000/public/viewer.html` 및 `/public/camera.html`을 브라우저에서 열어 수동으로 진행합니다.
- 새로운 기능을 추가하면 로그 출력(`log()`/`console.log`)을 활용해 연결 상태와 주요 이벤트를 확인할 수 있도록 유지하세요.
